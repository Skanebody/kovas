/**
 * Réexport du StatusPill UI canonique avec l'API attendue par le Hub Dossier.
 * Préserve la convention de spec (composant `StatusPill` dans hub/) sans dupliquer la logique.
 */
export { StatusPill } from '@/components/ui/status-pill'
export type { StatusPillProps } from '@/components/ui/status-pill'
